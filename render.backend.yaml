services:
  - type: web
    name: beer-counter-api
    env: node
    plan: free
    buildCommand: npm install
    startCommand: node server/server.js
    envVars:
      - key: NODE_ENV
        value: production
    disk:
      name: beer-data
      mountPath: /data
      sizeGB: 1 